<Window x:Class="LogiRGB.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogiRGB" UseLayoutRounding="True"
        mc:Ignorable="d" Loaded="Window_Loaded" Closing="Window_Closing" Topmost="True"
        Title="LogiRGB Settings" Icon="Images/LogiRGB.ico" Height="426" Width="240">
	<Window.Background>
		<RadialGradientBrush GradientOrigin="0.5,0" RadiusX="1.25" RadiusY="0.55" Center="0.5,0.5">
			<GradientStop Color="#3C3B3D" Offset="0" />
			<GradientStop Color="#1B1B1C" Offset="1" />
		</RadialGradientBrush>
	</Window.Background>
	<Window.Resources>
		<DropShadowEffect x:Key="DropShadow" ShadowDepth="1" BlurRadius="4" Direction="270" RenderingBias="Quality"/>
		<SkewTransform x:Key="OWSkew" AngleX="-15" />

		<LinearGradientBrush x:Key="HeaderInnerShadow" EndPoint="0,1">
			<GradientStop Color="#40000000" Offset="0" />
			<GradientStop Color="#00000000" Offset="0.1" />
			<GradientStop Color="#00FFFFFF" Offset="0.95" />
			<GradientStop Color="#40FFFFFF" Offset="1" />
		</LinearGradientBrush>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="30" />
			<RowDefinition Height="128" />
			<RowDefinition Height="30" />
			<RowDefinition Height="100" />
			<RowDefinition Height="30" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Border Background="{StaticResource HeaderInnerShadow}">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="11" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<Border Grid.Column="1" Background="#FF6000" RenderTransform="{StaticResource OWSkew}" />
				<Border Grid.Column="2" Background="#00FF42" RenderTransform="{StaticResource OWSkew}" />
				<Border Grid.Column="3" Background="#0080FF" RenderTransform="{StaticResource OWSkew}" />

				<Label Grid.Column="4" Content="Color Preview" Foreground="White" FontSize="16"
						Padding="0" VerticalAlignment="Center" Margin="8,0,0,0" Effect="{StaticResource DropShadow}" />
			</Grid>
		</Border>


		<Border Grid.Row="1" Margin="4" CornerRadius="4" x:Name="colorBorder" />
		<Label Grid.Row="1" Content="#FFFFFF" x:Name="colorLabel" Foreground="White" Effect="{StaticResource DropShadow}"
				HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,4,0,0"
				FontSize="20" />

		<Border Grid.Row="2" Background="{StaticResource HeaderInnerShadow}">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="11" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition Width="8" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>

				<Border Grid.Column="1" Background="#FF6000" RenderTransform="{StaticResource OWSkew}" />
				<Border Grid.Column="2" Background="#00FF42" RenderTransform="{StaticResource OWSkew}" />
				<Border Grid.Column="3" Background="#0080FF" RenderTransform="{StaticResource OWSkew}" />

				<Label Grid.Column="4" Content="Settings" Foreground="White" FontSize="16"
						Padding="0" VerticalAlignment="Center" Margin="8,0,0,0" Effect="{StaticResource DropShadow}" />
			</Grid>
		</Border>

		<ListView Grid.Row="3" />
	</Grid>
</Window>
